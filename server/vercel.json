{
    "version": 2,
    "builds": [
        {
            "src": "server.js",
            "use": "@vercel/node",
            "config": {
                "includeFiles": [
                    "routes/**",
                    "models/**",
                    "utils/**",
                    "middleware/**"
                ]
            }
        }
    ],
    "routes": [
        { "src": "/api/(.*)", "dest": "server.js" }
    ]
}